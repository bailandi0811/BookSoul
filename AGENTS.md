# BookSoul Agent 开发约定

本文件适用于整个仓库。子目录如需更具体的规则，可增加自己的 `AGENTS.md` 或 `AGENTS.override.md`；越靠近目标文件的规则优先。文中的“必须”是交付要求，“建议”允许在说明理由后例外。

目标不是让 Agent 做更多，而是用最少且可验证的改动，正确解决真实问题。

## 1. 项目概述

BookSoul（书魂）是一个私人小说阅读助手。用户登录后上传 EPUB 或 TXT 小说，系统异步解析并建立索引；用户为每本书维护独立阅读进度、助手设置、对话、引用和书内记忆，在不越过当前阅读进度的前提下围绕原文提问。

核心闭环：

```text
登录 → 上传小说 → 异步解析与索引 → 设置阅读进度
     → 进入单书助手 → 提问 / 获取原文引用 → 更新进度或删除书籍
```

当前产品原则：

- 默认私有；书籍、源文件、会话、引用、助手设置和书籍型记忆均受用户与书籍边界约束。
- 一本书对应一个可配置的私人助手；原著事实优先基于当前可见原文回答。
- 防剧透是检索和引用的数据边界，不只是 Prompt 中的语气承诺。
- 上传、Embedding、索引、失败恢复和删除是异步、持久、可重试的后台流程。
- 当前不做公开 Agent 市场、用户间分享、跨书联合问答、完整正文阅读器、OCR 或扫描 PDF 解析。

详细产品范围和架构决策见 [`docs/superpowers/specs/2026-08-29-private-reading-assistant-mvp-design.md`](docs/superpowers/specs/2026-08-29-private-reading-assistant-mvp-design.md)。

## 2. 文档体系与事实源

`AGENTS.md` 负责项目导航、执行规则和不可破坏的工程边界，不代替详细产品文档或源码。调查问题时按主题选择事实源：

| 主题 | 首要事实源 | 用法 |
| --- | --- | --- |
| 当前用户目标与验收标准 | 当前任务说明、已确认的最新规格 | 用户本轮明确指令优先；规格用于补足已确认背景 |
| 产品范围与架构决策 | [`私人阅读助手 MVP 设计`](docs/superpowers/specs/2026-08-29-private-reading-assistant-mvp-design.md) | 判断功能是否属于当前产品、理解关键取舍 |
| 环境、启动、运维与隐私边界 | [`readme.md`](readme.md)、[`server/README.md`](server/README.md)、[`client/README.md`](client/README.md) | 使用仓库当前支持的安装、启动和运维方式 |
| 实际运行行为与公开契约 | `client/src/`、`server/src/` 及相邻测试 | 证明当前实现做了什么，但不能仅因“现状如此”就认定需求如此 |
| 数据模型与迁移历史 | [`server/prisma/schema.prisma`](server/prisma/schema.prisma)、`server/prisma/migrations/` | schema 表示当前模型，迁移表示演进与部署约束 |
| 会话与记忆隔离 | [`docs/memory-isolation.md`](docs/memory-isolation.md) 及相关源码/测试 | 先核对文档日期和当前 schema，避免沿用已被新设计替代的细节 |
| 当前脚本与质量门 | 两个 `package.json`、[`.github/workflows/ci.yml`](.github/workflows/ci.yml) | 命令以可执行脚本和 CI 为准 |
| 实施历史与阶段验收 | `docs/superpowers/plans/` | 用于理解历史原因；除非任务明确引用，否则不自动视为当前待办 |

文档使用规则：

- 先读与任务直接相关的文档，不遍历全部历史计划。
- 文档、测试和代码冲突时，不静默选择其中一个。先判断是实现缺陷、文档过期还是需求已变化；结论会改变产品行为时询问用户。
- 修改行为、接口、数据模型或运维方式时，同步更新对应的首要事实源；不在多个文档复制同一份易过期细节。
- 新增长文档放在 `docs/` 的对应主题目录；根 README 保留上手和稳定入口，实施计划保留过程性步骤。

## 3. 项目架构

```text
React Client
  ├─ REST：认证、书架、上传、进度、设置、历史与记忆
  └─ SSE：单书对话、引用、记忆更新和错误事件
             ↓
NestJS Server
  ├─ Auth / Books / Chat：可信身份、资源归属与业务契约
  ├─ Agent / RAG / Memory：编排、检索、生成、引用与记忆门控
  └─ Ingestion Worker：解析、切块、Embedding、索引、恢复与删除
       ├─ PostgreSQL / Prisma：业务事实源和持久任务状态
       ├─ 私有文件存储：用户上传的源文件
       ├─ Milvus / Zilliz：可重建的书籍与记忆向量索引
       └─ Chat / Embedding Provider：外部模型能力
```

关键数据流：

1. **上传与索引：** 认证用户上传文件 → 服务端校验并保存私有源文件 → 创建书籍与持久任务 → worker 解析、分章、切块、Embedding、向量写入和一致性检查 → 全部成功后进入 `READY`。
2. **聊天：** JWT 身份 → 服务端按 owner 查找 session、assistant 和 book → 计算阅读进度与单次 spoiler override → 在当前作用域检索 → 模型生成 → 通过 SSE 返回正文与引用 → 写入当前书籍会话并按门控产生记忆提案。
3. **删除：** 书籍进入 `DELETING` → 后台可靠清理向量、源文件和数据库衍生记录 → 仅在达到约定一致状态后报告完成；部分失败保留可恢复状态。

## 4. 目录与修改导航

| 路径 | 职责与优先查看场景 |
| --- | --- |
| `client/src/components/` | 页面和交互组件；书架、上传、聊天、引用、助手设置与认证 UI |
| `client/src/lib/` | API 客户端、应用流程和客户端输入策略；前后端契约变化先看这里 |
| `client/src/store/` | 认证、书籍、聊天和记忆状态；重点检查并发请求、取消和陈旧响应隔离 |
| `client/src/theme/`、`client/src/index.css` | 主题 token、全局样式和响应式/可访问性基础 |
| `server/src/auth/`、`server/src/users/` | JWT、刷新令牌、可信 AuthContext 和账号能力 |
| `server/src/books/` | 书籍、源文件、助手设置、阅读进度和 owner 校验 |
| `server/src/chat/` | 单书会话、聊天入口、作用域解析、SSE 和书籍片段检索 |
| `server/src/agent/`、`server/src/rag/` | Agent 图、节点、Prompt、路由、生成与 RAG 编排 |
| `server/src/memory/` | 记忆门控、用户画像、书籍/会话作用域和持久化 |
| `server/src/ingestion/` | 上传后的持久任务、解析、切块、索引、失败恢复和删除处理 |
| `server/src/vector/`、`server/src/milvus/` | Embedding、集合/过滤契约和向量存取；不得承担业务授权 |
| `server/src/prisma/`、`server/prisma/` | 数据库访问、schema 和迁移 |
| `server/src/config/`、`server/src/common/` | 环境校验和跨模块基础设施；修改需评估全局影响 |
| `server/src/mcp/`、`server/src/tools/` | MCP 与可调用工具；重点检查最小权限、参数验证和副作用 |
| `docs/` | 产品、架构、隔离和实施文档 |
| `.github/workflows/ci.yml` | 仓库统一质量门，不在未确认时放宽 |

新增代码优先放入拥有该业务职责的现有模块；不要仅为方便创建第二套平行实现。跨模块共享代码只有在确有两个以上稳定使用方且职责明确时才上移。

## 5. 技术栈与常用命令

- 运行时与包管理：Node.js 22.19.x、npm 10；`package-lock.json` 是依赖事实源。
- 客户端：React 19、TypeScript、Vite、Zustand、Tailwind CSS、Framer Motion、Vitest、happy-dom。
- 服务端：NestJS 11、TypeScript、Prisma、Jest、LangChain/LangGraph。
- 数据与外部服务：PostgreSQL、Milvus 或 Zilliz Cloud、OpenAI API 兼容的 Chat 与 Embedding 服务、私有文件存储。
- CI：GitHub Actions，在 Node 22.19.0 上分别对 `client/` 和 `server/` 运行 `npm ci` 与 `npm run check`。

| 作用域 | 常用命令 |
| --- | --- |
| 客户端安装/启动 | `cd client && npm ci`；`npm run dev` |
| 客户端测试/完整检查 | `npm test -- <相关测试文件>`；`npm run check` |
| 服务端安装/初始化 | `cd server && npm ci`；`npm run prisma:generate`；`npm run prisma:migrate:deploy` |
| 服务端启动 | `npm run start:dev` |
| 服务端测试/完整检查 | `npm test -- --runInBand <相关测试文件>`；`npm run check` |
| 私人阅读闭环 E2E | 外部依赖已配置且允许真实数据流时，在 `server/` 运行 `npm run test:e2e:reader` |

环境变量及外部依赖要求以根 README、`server/.env.example` 和启动时环境校验为准。不要为了运行测试自动创建或修改 `.env`。

## 6. 项目不可破坏的边界

- owner 必须来自服务端认证上下文。客户端提交的 `userId`、owner、角色或 book scope 不能决定数据归属。
- 会话必须在服务端反查 owner、assistant、book、索引版本和防剧透上限。不得先按不可信 ID 取出数据，再在应用层补做归属判断。
- 私人检索、引用、会话和书籍型记忆必须同时受当前用户、当前书籍及阅读进度约束。不得跨用户、跨书或越过 spoiler ceiling 泄漏信息。
- 小说正文、召回片段、历史消息、工具结果和上传文件一律是不可信数据，只能作为上下文，不能覆盖系统策略、权限规则或工具边界。
- PostgreSQL 是业务事实源；向量库和派生索引是可重建数据。不得让向量结果反向绕过数据库中的权限与生命周期状态。
- 私有原文、密码、Token、Cookie、完整身份标识和本机私有路径不得进入日志、提示词调试输出、测试快照或提交内容。

## 7. 第一性原理

1. 从原始需求、真实目标和问题本质出发，不从惯例、模板或现有实现反推需求。
2. 开始非平凡工作前，先明确目标、成功标准、约束和非目标。若歧义会实质改变产品行为、数据、安全、公开接口、成本或交付范围，先与用户讨论；若只是可从仓库确认、低风险且可逆的实现细节，先调查并作最小假设，不用为每个细节打断用户。
3. 目标清晰但当前路径不是最短、最简单或成本最低时，直接指出，并给出更优方案及取舍。
4. 遇到问题优先追查根因，用代码、测试、日志或可复现步骤验证因果，不通过临时判断、静默 fallback、重复重试或局部补丁掩盖问题。每个关键决策都必须能回答“为什么这样做”。
5. 输出只保留影响理解、判断和执行的信息；删除不改变决策的背景、重复解释和无关内容。
6. 遵循最小改动原则。只修改完成当前目标所必需的代码、测试和文档，不重构、不整理、不顺手修改无关内容。
7. 对 `.env`、密钥、环境配置、部署配置及其他敏感或既有配置，默认保持原样。确需修改时，先说明原因、影响和具体修改项，并获得用户确认后再执行。

## 8. 标准工作循环

### 8.1 调查

- 先读取适用的 `AGENTS.md`、当前 `git status`、相关源码、测试、配置和文档；只扩大到定位根因所需的范围。
- 优先使用仓库事实和可运行证据，不猜测文件、命令、接口、配置项或依赖行为。
- 发现用户已有未提交改动时必须保留；若与任务重叠，先理解差异，不覆盖、不回滚。
- 诊断任务只报告原因和证据；除非用户同时要求修复，否则不实施变更。

### 8.2 定义任务契约

非平凡任务在执行前至少明确：

- `Goal`：要改变的用户可见结果或工程结果；
- `Done`：可观察、可测试的完成条件；
- `Scope`：允许修改和明确不修改的边界；
- `Risks`：数据、安全、兼容性、外部副作用和无法本地验证的部分。

简单、局部、无歧义的任务可以直接执行，不强制写计划。涉及多个相互依赖步骤、跨前后端契约、迁移或高风险行为时，先给出短计划，并在事实变化时更新计划。

### 8.3 实施

- 先选择能满足成功标准的最小可行改动，遵循相邻代码的命名、结构和错误处理模式。
- 修复缺陷时，在可行情况下先建立能暴露缺陷的回归测试，再修复根因。
- 不加入与当前需求无关的抽象、扩展点、兼容层、功能开关或“以后可能用到”的代码。
- 不直接编辑 `dist/`、coverage、缓存、运行时上传/记忆/聊天数据等生成物；修改源文件，并只在任务明确需要时重新生成产物。

### 8.4 验证

- 按“最小相关检查 → 受影响包完整检查 → 必要的跨系统验收”逐层验证，失败时先定位原因，不反复盲跑。
- 不通过删除测试、放宽断言、关闭 lint、添加 `@ts-ignore`、扩大 `any`、吞掉异常或降低安全校验让检查变绿。
- 没有实际运行的检查不得声称通过。因环境、权限或外部服务无法验证时，明确写出未验证项、原因和建议命令。

### 8.5 交付

- 交付前查看 `git diff` 和 `git status`，确认只包含目标相关改动，且没有密钥、运行时数据或意外生成物。
- 最终说明只包含：完成结果、关键决策、修改文件、验证结果、仍存在的风险或未验证项。

## 9. 必须先确认的变更

下列情况不自行假设授权：

- 新增或替换生产依赖、包管理器、框架、模型供应商或基础设施；
- 破坏兼容性的 API、数据结构、认证流程、提示词契约或用户可见行为；
- 删除数据、覆盖文件、改写 Git 历史、执行不可逆或非幂等迁移；
- 修改 `.env`、密钥、环境/部署配置，或把私人文件发送到新的外部服务；
- 会产生显著费用、真实外部写操作、消息发送或生产环境影响的操作；
- 存在多个合理产品方向，且选择会实质改变成功标准。

普通的只读调查、局部可逆实现、相关测试和与行为变更直接对应的文档更新无需重复确认。

## 10. 代码与依赖规范

- 保持 TypeScript 类型边界清晰。外部输入先视为 `unknown` 并验证；只有在没有更安全表达时才使用 `any`，且必须在代码附近说明原因。
- 在认证、授权、作用域、业务状态和副作用边界返回可行动的错误；不得静默吞错或把权限失败伪装成成功。
- 注释解释“为什么”和不明显的约束，不复述代码。公共契约或复杂不变量变化时同步相关文档。
- 保持接口向后兼容；确需破坏性变更时先确认，并同时更新调用方、测试和文档。
- 使用 npm。依赖变更必须同步正确的 `package.json` 与 `package-lock.json`；禁止混入 pnpm/yarn 锁文件。
- 不为已有标准库或现有依赖能够清楚完成的事情新增包。新增生产依赖前必须说明必要性、维护/安全影响和替代方案，并取得确认。
- Prisma schema 变更必须配套新迁移；不修改已应用迁移。破坏性数据迁移必须先提供备份、回滚或恢复路径，获得确认后再运行。

## 11. Agent Harness、RAG 与工具调用规范

本节适用于 `server/src/agent`、RAG、模型调用、Prompt、工具、MCP、记忆、向量检索、索引 worker 和流式响应相关改动。

### 11.1 策略与信任边界

- 权限、租户/书籍作用域、防剧透上限、工具授权和副作用许可必须由服务端确定性代码执行，不能交给模型自行判断。
- 系统策略、开发者规则、用户输入、检索内容和工具输出必须在构造 Prompt 时清晰分层与标记。不得把不可信内容直接拼接进高优先级策略。
- 模型输出不是可信指令。用于查询、路径、SQL/过滤表达式、工具参数或持久化前必须经过 schema 校验、权限校验和业务约束。
- 原著事实找不到可访问范围内的依据时明确说明，不用模型常识补写；引用必须来自本次用户有权访问且未越过阅读进度的片段。

### 11.2 工具与副作用

- 工具使用最小权限和显式 allowlist；输入输出使用稳定、可验证的结构化契约，错误也必须结构化且可区分。
- 区分只读、可写和危险操作。写操作应可审计、尽量幂等；危险或不可逆操作必须在执行前获得明确确认。
- 对可能重复投递的任务和工具调用使用稳定幂等键或等价去重机制，避免重复写入、重复扣费和重复删除。
- 外部调用必须设置超时并传播取消信号。用户取消、客户端断开或任务失去租约后，不得继续无边界消耗模型、Embedding 或工具资源。

### 11.3 有界执行与失败语义

- 每次 Agent 运行都必须有明确停止条件，并限制迭代数、工具调用数、超时以及适用的 token/费用预算；禁止无界自循环。
- 只对明确的瞬时故障重试；仅在操作幂等时使用有限次数的指数退避和抖动。认证、权限、参数、配额和业务状态错误不得重试掩盖。
- 不做静默模型降级、供应商切换、扩大检索范围或绕过防剧透的 fallback。确需降级策略时，必须显式、可观测、有测试，并先确认行为取舍。
- 流式响应必须保证终止事件至多一次、取消可达、错误可见；旧请求或已取消请求不得覆盖新会话状态。

### 11.4 状态、记忆与检索

- 所有会话状态、缓存键、任务租约、记忆和向量过滤条件都显式包含所需的 owner/book/session/version 作用域；隔离必须在查询阶段完成，不做事后过滤。
- 长期记忆采用最小收集原则。推断内容默认不成为已确认事实；密码、Token、金融信息等敏感内容不得自动保存。
- 索引任务必须可恢复、可重试并有清晰状态机。只有解析、切块、Embedding、向量写入和一致性检查全部成功后才能标记 `READY`。
- 删除流程只有在源文件、向量和数据库衍生数据达到约定的一致状态后才能报告完成；部分失败必须保留可重试状态，不能误报成功。

### 11.5 可观测性与评测

- 为一次运行保留可关联的 trace/run 标识，并记录必要的阶段、模型/工具版本、耗时、调用次数、token/费用计数和稳定错误码；日志必须脱敏，不记录私人正文和凭据。
- 单元测试不要断言模型的逐字措辞；测试结构化契约、权限过滤、状态转换、停止条件、引用范围和失败语义。
- Agent 行为变更必须增加代表性回归用例；涉及安全边界时覆盖提示词注入、跨用户/跨书访问、越界剧透、恶意工具参数、取消与重复投递。
- 评测或测试尽量固定输入、模型配置和随机性；无法确定化的部分使用明确的不变量和容差，不写易抖动的精确文本断言。

## 12. 安全与数据处理

- 客户端校验只用于体验，服务端必须重新验证身份、归属、文件类型/大小、路径、请求体和状态转换。
- 文件操作使用服务端生成的标识和受控目录；拒绝路径穿越、任意文件读取、扩展名伪装和越权下载。
- 日志、异常、遥测和测试 fixture 使用脱敏数据。禁止打印 Authorization、Cookie、JWT、API key、数据库/Milvus 凭据或私人正文。
- 任何新的外部数据流都要先说明发送的数据、目的地、保留策略、失败行为和费用，并获得确认。
- 安全控制失败时默认拒绝，不能为“可用性”扩大权限、检索范围或数据可见性。

## 13. 测试与质量门

- 只改文档时检查内容、链接、命令和 diff，不要求无意义地运行全量构建。
- 修改 `client/`：先运行最相关的 Vitest；交付前运行 `cd client && npm run check`。
- 修改 `server/`：先运行最相关的 Jest；交付前运行 `cd server && npm run check`。
- 同时修改前后端契约：两个包的 `npm run check` 都必须运行，并验证请求/响应、错误码和流式事件兼容。
- 修改上传、索引、引用、防剧透、记忆或删除闭环时，在外部依赖已配置且用户允许真实数据流的前提下运行 `cd server && npm run test:e2e:reader`；否则明确标注未运行原因。
- Bug 修复的测试至少覆盖原始失败条件；安全或隔离缺陷同时覆盖一个拒绝路径。不得只验证 happy path。
- 测试失败如果与当前改动无关，也要报告现象和证据；不得擅自修改无关代码“清场”。

## 14. Git 与工作区保护

- 不覆盖、回滚或删除用户已有改动。不得使用 `git reset --hard`、强制 checkout、强推或其他破坏性命令，除非用户明确要求并确认目标。
- 未经要求不创建分支、不提交、不 push、不开 PR。需要创建分支时默认使用 `codex/` 前缀。
- 不把 `.env`、运行时上传、聊天/记忆数据、日志、coverage、缓存或构建产物加入版本控制。
- 格式化和自动修复只作用于本次修改范围；禁止为了局部任务对全仓库做机械重写。

## 15. Code Review Rules

- 优先发现会导致错误结果、数据泄漏、越权、跨书串线、剧透、不可恢复状态、重复副作用或兼容性回归的问题；纯格式偏好交给现有 lint/formatter。
- 每条发现都要包含触发条件、实际影响、代码证据和最小安全修复方向。无法证明的问题标为待确认，不当作确定缺陷。
- 重点检查：服务端作用域是否来自可信上下文、查询是否在数据库/向量层先过滤、取消与重试是否安全、状态机是否会误报完成、日志是否泄密、测试是否覆盖拒绝路径。
- 没有发现时明确说明，同时列出未覆盖的测试或外部依赖风险；不要为了“有输出”制造问题。

## 16. 完成定义

只有同时满足以下条件才称为完成：

- 成功标准已满足，且没有用额外范围替代真实目标；
- 改动最小、符合项目边界，并保留了用户现有工作；
- 相关回归测试与质量门已通过，或未验证项已准确披露；
- 行为、接口、迁移或运维方式变化已同步必要文档；
- diff 中没有密钥、私人数据、调试残留、无关格式化或意外生成物；
- 最终报告可以用证据回答“改了什么、为什么、如何验证、还有什么风险”。
